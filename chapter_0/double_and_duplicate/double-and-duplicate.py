# Insert your double_and_duplicate() function here.
def double_and_duplicate(x: float) -> tuple[float, float]:
    """
    Double a decimal value and return two copies of the result.
    Args:
        x: Input decimal (float) value.
    Returns:
        A 2-tuple containing the doubled value twice: (2*x, 2*x).
    """
    cTuple = (2 * x, 2 * x)

    return cTuple
